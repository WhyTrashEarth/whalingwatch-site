---
import "../styles/global.css";
import SEO from "../components/SEO.astro";
import SkipLink from "../components/SkipLink.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

export type Props = {
  title?: string;
  description?: string;
  canonical?: string;
  ogImage?: string;
  noIndex?: boolean;
};

const { title, description, canonical, ogImage, noIndex } = Astro.props;
---
<!doctype html>
<html lang={Astro.currentLocale ?? "en"}>
  <head>
    <SEO title={title} description={description} canonical={canonical} ogImage={ogImage} noIndex={noIndex} />
  </head>
  <body>
    <SkipLink />
    <Header />

    <main id="main-content" class="container" role="main" tabindex="-1" style="padding: 0.5rem 0 2rem;">
      <slot />
    </main>

    <Footer />
  </body>
</html>